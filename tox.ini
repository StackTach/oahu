[tox]
envlist = py26,py27

[testenv]
deps = 
    coverage
    nose
    notigen
    notification_utils
    mock
    pymongo

commands = nosetests -d -v --with-coverage --cover-inclusive --cover-package oahu []
